# ============================================================================
# 🌟 PITCHCONNECT - Environment Configuration Template
# ============================================================================
#
# This file shows all available environment variables.
# Copy this to .env.local and fill in your actual values.
#
# CRITICAL: .env.local is in .gitignore and should NEVER be committed!
#
# ============================================================================

# ============================================================================
# NEXTAUTH CONFIGURATION (Required)
# ============================================================================

# JWT Secret - Generate with: openssl rand -base64 32
# This signs all JWT tokens. Change in production!
NEXTAUTH_SECRET=generate-random-secret-here

# NextAuth callback URL - Update for your domain
# Local development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# Optional: For non-root deployment
# NEXTAUTH_URL_INTERNAL=http://localhost:3000

# ============================================================================
# DATABASE (Required)
# ============================================================================

# PostgreSQL connection string (Supabase, AWS RDS, etc.)
# Format: postgresql://user:password@host:port/database?schema=public
DATABASE_URL=postgresql://user:password@host:5432/dbname?schema=public

# Direct URL for database migrations (optional)
# Use if DATABASE_URL is behind a proxy
DIRECT_URL=postgresql://user:password@host:5432/dbname?schema=public

# ============================================================================
# OAUTH PROVIDERS (Optional - but recommended)
# ============================================================================

# Google OAuth
# Get from: https://console.cloud.google.com
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
# Get from: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ============================================================================
# STRIPE (Optional - for payments)
# ============================================================================

# Stripe API keys
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_PUBLIC_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_test_...

# Product/Price IDs for subscriptions
STRIPE_PRODUCT_BASIC=prod_...
STRIPE_PRODUCT_PRO=prod_...
STRIPE_PRODUCT_ENTERPRISE=prod_...

# ============================================================================
# EMAIL (Optional - for notifications)
# ============================================================================

# SendGrid
SENDGRID_API_KEY=SG...

# Or use your own SMTP server
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# ============================================================================
# VIDEO PROCESSING (Optional - for video features)
# ============================================================================

# Mux for video streaming and processing
# Get from: https://dashboard.mux.com/settings/tokens
MUX_TOKEN_ID=your-mux-token-id
MUX_TOKEN_SECRET=your-mux-token-secret

# ============================================================================
# MONITORING & ANALYTICS (Optional)
# ============================================================================

# Sentry for error tracking
SENTRY_AUTH_TOKEN=your-sentry-token
NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...

# Google Analytics (public)
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-...

# PostHog for product analytics (public)
NEXT_PUBLIC_POSTHOG_KEY=phc_...
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ============================================================================
# API & EXTERNAL SERVICES
# ============================================================================

# Public API URL (used in client-side requests)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Internal API URL (for server-to-server calls)
INTERNAL_API_URL=http://localhost:3000

# External services (if any)
# OPENAI_API_KEY=sk-...
# HUGGING_FACE_API_KEY=hf_...

# ============================================================================
# FEATURE FLAGS (Optional)
# ============================================================================

# Enable demo/test mode
NEXT_PUBLIC_DEMO_MODE=false

# Feature toggles
NEXT_PUBLIC_ENABLE_VIDEOS=true
NEXT_PUBLIC_ENABLE_AI=true
NEXT_PUBLIC_ENABLE_REAL_TIME=true
NEXT_PUBLIC_ENABLE_ANALYTICS=true

# ============================================================================
# DEVELOPMENT SETTINGS (Development Only)
# ============================================================================

NODE_ENV=development

# Debug modes
DEBUG=false
NEXTAUTH_DEBUG=false
DEBUG_SQL=false

# Keep sensitive logs only in development
NEXT_PUBLIC_LOG_LEVEL=debug

# ============================================================================
# INSTRUCTIONS FOR SETUP
# ============================================================================
#
# 1. LOCAL DEVELOPMENT:
#    - Copy this file to .env.local
#    - Fill in DATABASE_URL with your local/dev database
#    - Optional: Fill in OAuth credentials for faster setup
#    - Run: npm install
#    - Run: npm run prisma:push (to sync database schema)
#    - Run: npm run dev
#
# 2. STAGING/PRODUCTION DEPLOYMENT:
#    - Use your deployment platform's environment variables
#    - For Vercel: Settings → Environment Variables
#    - For AWS: Secrets Manager or Parameter Store
#    - For Docker: Pass via docker-compose.yml or .env file
#    - Never commit .env files to version control
#
# 3. GENERATING SECURE RANDOM SECRETS:
#    - NEXTAUTH_SECRET: openssl rand -base64 32
#    - JWT_SECRET: openssl rand -base64 32
#    - API_KEY: openssl rand -hex 32
#
# 4. DATABASE MIGRATION:
#    - Local dev: npm run prisma:migrate (creates new migration)
#    - Production: npm run prisma:migrate:prod (applies migrations)
#
# 5. TESTING:
#    - Create .env.test.local for test environment
#    - Use test database URL
#    - Disable external API calls in tests
#
# ============================================================================
